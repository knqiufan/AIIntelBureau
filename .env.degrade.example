# Offline/degraded demo profile overlay. It keeps deterministic answers while
# retaining the normal API shape; set direct OceanBase/MySQL-compatible values
# in .env if real memory is used.
DEMO_MODE=degrade
DEMO_MEMORY_ADAPTER=production
DEMO_WARMUP=false
SEEKDB_MODE=oceanbase
VITE_DEMO_DATA_SOURCE=api
