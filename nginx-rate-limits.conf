# This file is included by nginx from the http context before server blocks.
# The key is the immediate peer address; the API applies a second, durable
# per-session quota after authentication.
limit_req_zone $binary_remote_addr zone=api_per_client:10m rate=20r/s;
limit_conn_zone $binary_remote_addr zone=per_client:10m;
